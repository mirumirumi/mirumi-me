{
  "name": "shared",
  "private": true,
  "scripts": {
    "tsc": "tsc --noEmit",
    "test": "vitest run --config ../vitest.config.ts --configLoader runner shared"
  },
  "exports": {
    "./*": {
      "types": "./src/*.ts",
      "import": "./src/*.ts"
    }
  },
  "dependencies": {
    "@notionhq/client": "^5.25.2",
    "zod": "^4.4.3"
  }
}
