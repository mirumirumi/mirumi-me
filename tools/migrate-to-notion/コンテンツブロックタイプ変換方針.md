## 概要

- 可視化は下記の Figma：
    - https://www.figma.com/design/RCGMU4H8OcB3lBDo5MmB23/mirumi.me?node-id=3311-6&t=cNLJSx2PkYzCBKic-4
    - 上記で課題管理してるが最終成果はこっちに転記する

- このドキュメントで扱うのはあくまでも既存の DB 本文から Notion 上の表現としてどのブロックタイプに持っていくかのスコープだが、Worers API 側で新しく変換したりするロジックがどれに必要なのかのリストが他にないため、🔵 マークで定義する


## ブロック要素



- X のリンクだけ生 URL 値になっているが、これもショートコードにしたほうがいいかを考える
    - そもそもいまの WordPress 側の実装に依存しているだけな気がするから理想ベースで考えていいと思う🔴


## インライン系

- Notion には文字色変更が 9 色、ハイライトも 9 色ある

- インライン span
    - フォントサイズ系
        - xxx
    - 色系
        - xxx
    - 背景色系
        - xxx
    - underline
        - 
    - 複数の style が混ざってるのは専用にクエリしたほうがよさそう🔴
        - xxx

- `<del>`
    - 標準

- `<code>`
    - 標準

- `<em>`、もしかしたら `<i>` もあるかも
    - 標準（斜体）

- `<sup>`
    - 標準（インライン数式）


## ショートコード

- まず先に音声、動画、iframe あたりの埋め込みを先にどうにかする

- `[amazon` から始まる商品リンクはそのまま持っていく🔵
    - rakuten は既存記事ではゼロにした

- 残ったものがブログカード
    - それ以外の雑多なショートコードやショートコードですらないものはすでに消してある
    - ショートコードと機能するべきものでインラインのものは残っていないはずなので `<p>[xxx` で探す→単に標準テキストでそのまま置き換えればいい（`/` 始まりもなるべくフル URL にしたいが）
        - p じゃなくて div で囲まれているものもあるっぽいので注意！
        - ソースコード内のものも `[]` の文字列は残ってるけどこれはオンリー段落じゃないから大丈夫なはず
        - あと途中で `[]` を撲滅する作業はやめたから文中の普通の文字列として使われているやつはまだ残っているから注意してね


## 例外系

- 埋め込み css
    - `<style>` で Search Regex すればまだ対応方針決められてなくて残ってるやついっぱい出てくるよ
        - `custom-css` っていうプロパティつくってここに直接 css 書けばいい（ただしもともとレンダリングされる class にしかもちろん使えないけどね）


## その他

- テーブルにいろんな細かいカスタマイズパターンがあるがどうすればいい…？🔴
    - big-car-navi-compatible-model
        - sticky の css が記事内にある
    - あとミニテーブルは別 UI 扱い！🔴

- Search Regex でミスって標準テキスト段落なのに p タグで囲まれていないものがけっこうありそうなので考慮して（少なくとも今回もうやった）

- 画像のサイズ調整
    - ショートコードで `[img name="{fileName}" width="" (align="")]` とかってやるしかないかな…
        - インライン画像もこのショートコードで対応する
